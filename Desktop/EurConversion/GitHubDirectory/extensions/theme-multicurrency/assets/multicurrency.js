// assets/multicurrency.js
// Може да форматираш цените с Intl.NumberFormat, 
// но ако не ти трябва JavaScript – остави файла празен или го махни.
document.addEventListener('DOMContentLoaded', () => {
  // пример: взимаш всички <small> стойности и ги форматираш
  // const elems = document.querySelectorAll('.multicurrency-order-totals small');
  // elems.forEach(el => {
  //   const value = parseFloat(el.textContent);
  //   el.textContent = new Intl.NumberFormat('bg-BG', {
  //     style: 'currency',
  //     currency: el.textContent.includes('€') ? 'EUR' : 'BGN'
  //   }).format(value);
  // });
});
